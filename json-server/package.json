{
  "devDependencies": {
    "tailwindcss": "^3.4.4"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.1"
  },
  "scripts": {
    "start": "npx json-server --watch db.json --port 3001",
    "build": "npx tailwindcss -i ./dist/input.css -o ./dist/output.css --watch"
  }
}
